import React from "react";
import { Ionicons } from '@expo/vector-icons';

export default function IconeFechar(props){
    return(
    <Ionicons name="md-close-outline" size={28} color={props.color} />    )
}